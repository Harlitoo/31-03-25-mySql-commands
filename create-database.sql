create database `harley`;
# a linha acima serve pára criar um banco de dados com o nome: `Harley`
# o uso da crase se torna neessário para indicar uma `string`do sistema
# `strings`do sistema: `database, `table` e `campo`
